#!/bin/bash

echo  "hi from command"
echo  "giving script as command in linux"

#running this script as command
#sudo cp 27.commmand.sh /usr/bin/linuxcommand #copy this script into/usr/bin path and linuxcommand is just name
#sudo chmod +x /usr/bin/linuxcommand --> giving execute permission
#if we run command as sudo linuxcommand
#we get output in teminal as  hi from command
        #giving script as command in linux